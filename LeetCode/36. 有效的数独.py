class Solution:
    def isValidSudoku(self, board: List[List[str]]) -> bool:
        def isRepear(arr, dimention):
            if dimention == 1:
                for i in range(i):
                    if 
        # 9行 9列 9个3x3的
        for i in range(9):
            rows = board[i]
